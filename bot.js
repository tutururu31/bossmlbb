const TelegramBot = require('node-telegram-bot-api');

// Токен бота (замените на свой)
const token = '8323888701:AAEOfkfk5Oncd1xSdFeUVKfTe0-655_MArc';

// URL вашего мини-приложения (замените на свой)
const webAppUrl = 'https://tutururu31.github.io/bossmlbb/';

// Создаем бота
const bot = new TelegramBot(token, { polling: true });

// Обработчик команды /start
bot.onText(/\/start/, (msg) => {
    const chatId = msg.chat.id;
    const firstName = msg.from.first_name;
    
    const welcomeText = `Привет, ${firstName}! 🎮\n\n` +
        'Добро пожаловать в *MLBB Битва с Боссами*!\n\n' +
        '🎯 *Особенности игры:*\n' +
        '• Сражайся с боссом на 100,000 HP\n' +
        '• Покупай улучшения в магазине\n' +
        '• Фарми монстров для золота\n' +
        '• Используй автокликер и способности\n\n' +
        'Нажми кнопку ниже, чтобы начать игру!';
    
    const options = {
        reply_markup: {
            inline_keyboard: [
                [{
                    text: '🎮 ИГРАТЬ СЕЙЧАС',
                    web_app: { url: webAppUrl }
                }],
                [{
                    text: '📖 Как играть?',
                    callback_data: 'how_to_play'
                }]
            ]
        },
        parse_mode: 'Markdown'
    };
    
    bot.sendMessage(chatId, welcomeText, options);
});

// Обработчик команды /game
bot.onText(/\/game/, (msg) => {
    const chatId = msg.chat.id;
    
    const options = {
        reply_markup: {
            inline_keyboard: [
                [{
                    text: '🎮 Открыть игру',
                    web_app: { url: webAppUrl }
                }]
            ]
        }
    };
    
    bot.sendMessage(chatId, 'Нажми кнопку ниже, чтобы открыть игру:', options);
});

// Обработчик команды /help
bot.onText(/\/help/, (msg) => {
    const chatId = msg.chat.id;
    
    const helpText = '🎮 *MLBB Битва с Боссами*\n\n' +
        '📖 *Основные команды:*\n' +
        '/start - Начать работу с ботом\n' +
        '/game - Открыть игру\n' +
        '/help - Помощь\n' +
        '/stats - Статистика (скоро)\n\n' +
        '🎯 *Как играть:*\n' +
        '1. Нажми "Играть сейчас"\n' +
        '2. Бей босса кликами\n' +
        '3. Зарабатывай золото на монстрах\n' +
        '4. Покупай улучшения\n' +
        '5. Победи босса!\n\n' +
        '📢 *Обновления и поддержка:*\n' +
        'По вопросам и предложениям пишите @tutururu31';
    
    bot.sendMessage(chatId, helpText, { parse_mode: 'Markdown' });
});

// Обработчик callback-запросов
bot.on('callback_query', (callbackQuery) => {
    const msg = callbackQuery.message;
    const chatId = msg.chat.id;
    const data = callbackQuery.data;
    
    if (data === 'how_to_play') {
        const howToPlayText = '🎮 *Как играть в MLBB Битва с Боссами:*\n\n' +
            '1. *Главный босс* - кликай по боссу, чтобы наносить урон\n' +
            '2. *Магазин* - покупай улучшения за золото:\n' +
            '   • Усиленный удар - больше урона за клик\n' +
            '   • Автокликер - автоматически бьет босса\n' +
            '   • Шанс крита - увеличивает вероятность критического удара\n' +
            '   • Сила крита - увеличивает множитель крита\n' +
            '   • Огненная аура - периодический урон\n' +
            '3. *Монстры* - убивай монстров, чтобы зарабатывать золото\n' +
            '4. *Статистика* - следи за своим прогрессом\n\n' +
            '💡 *Советы:*\n' +
            '• Сначала убей несколько монстров для золота\n' +
            '• Улучшай урон за клик\n' +
            '• Автокликер работает только на босса\n' +
            '• Золото нужно для всех улучшений\n\n' +
            'Удачи в игре! 🏆';
        
        bot.sendMessage(chatId, howToPlayText, { parse_mode: 'Markdown' });
    }
    
    // Ответим на callback, чтобы убрать "часики" у кнопки
    bot.answerCallbackQuery(callbackQuery.id);
});

// Обработчик обычных сообщений
bot.on('message', (msg) => {
    const chatId = msg.chat.id;
    const text = msg.text;
    
    // Игнорируем команды, которые уже обработаны
    if (text.startsWith('/')) return;
    
    // Ответ на любое другое сообщение
    bot.sendMessage(chatId, 'Используйте команды:\n/start - начало работы\n/game - открыть игру\n/help - помощь');
});

// Запуск бота
console.log('🤖 Бот запущен...');
console.log('👉 Перейдите в Telegram и найдите своего бота');
console.log(`🌐 Мини-приложение доступно по адресу: ${webAppUrl}`);
console.log('📝 Для остановки бота нажмите Ctrl+C');

// Обработка ошибок
bot.on('polling_error', (error) => {
    console.error('Ошибка polling:', error);
});

bot.on('webhook_error', (error) => {
    console.error('Ошибка webhook:', error);
});
